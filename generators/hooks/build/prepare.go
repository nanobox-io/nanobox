package build

// PreparePayload returns a string for the user hook payload
func PreparePayload() string {
	// currently, this payload is empty. This may change at some point
	return emptyPayload()
}
