package build

// ConfigurePayload returns a string for the user hook payload
func ConfigurePayload() (string, error) {
  // currently, this payload is empty. This may change at some point
  return "{}", nil
}
