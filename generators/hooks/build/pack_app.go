package build

// PackAppPayload returns a string for the user hook payload
func PackAppPayload() string {
	// currently, this payload is empty. This may change at some point
	return emptyPayload()
}
