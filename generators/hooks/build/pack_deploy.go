package build

// PackDeployPayload returns a string for the user hook payload
func PackDeployPayload() (string, error) {
	// currently, this payload is empty. This may change at some point
	return "{}", nil
}
