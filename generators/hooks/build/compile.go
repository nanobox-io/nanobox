package build

// CompilePayload returns a string for the user hook payload
func CompilePayload() string {
	// currently, this payload is empty. This may change at some point
	return emptyPayload()
}
